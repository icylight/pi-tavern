/**
 * pi-tavern — a gathering place for pi coding agent extensions, skills, and tools.
 */
export function greet(name: string): string {
  return `Welcome to the tavern, ${name}!`;
}

console.log(greet("pi"));
